[project]
name = "robust-llm-client"
version = "0.1.0"
description = "Robust LLM client with retry, fallback, usage tracking"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.50.0",
    "tenacity>=9.0.0",
    "python-dotenv>=1.0.0",
]
